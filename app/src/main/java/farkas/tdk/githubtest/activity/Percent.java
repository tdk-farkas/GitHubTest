package farkas.tdk.githubtest.activity;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

import farkas.tdk.githubtest.R;

/**
 * @author 唐地坤
 * Created by tangdikun on 16/1/7.
 */
public class Percent extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.item_line);

//        Toolbar toolBar = (Toolbar) findViewById(R.id.id_toolbar);
//        CollapsingToolbarLayout  collapsingToolbarLayout = (CollapsingToolbarLayout) findViewById(R.id.id_collapsingtoolbarlayout);
//        collapsingToolbarLayout.setTitle("标题");
//        collapsingToolbarLayout.setExpandedTitleColor(getResources().getColor(R.color.colorAccent));
//        collapsingToolbarLayout.setCollapsedTitleTextColor(getResources().getColor(R.color.main_white));
//        setSupportActionBar(toolBar);
    }
}
